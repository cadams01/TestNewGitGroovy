//New Git Repo for Week6
def my_colors = ["Purple", "blue", "yellow", "Green", "Orange", "White", "Black", "Teal", "Aqua", "red"]; 
for (color in my_colors) {
   if(color == "blue" || color == "red" || color == "yellow"){
       println("${color} is a primary color.")
   }else{
       println("${color} is not a primary color.")
   }
}

